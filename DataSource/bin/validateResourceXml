#!/usr/bin/perl

use lib "$ENV{GUS_HOME}/lib/perl";
use FgpUtil::Runtime::CommandHelper;

my $GUS_HOME = $ENV{GUS_HOME};
my $CLASSPATH = &FgpUtil::Runtime::CommandHelper::getJavaClasspath($GUS_HOME);
my $sysProps = &FgpUtil::Runtime::CommandHelper::getSystemProps($GUS_HOME, 'validateResourceXml');
if ($ARGV[0] != /^\//) {
  $ARGV[0] = "$ENV{PWD}/$ARGV[0]";
}
my $args = &FgpUtil::Runtime::CommandHelper::getJavaArgs(@ARGV);

my $cmd = "java $sysProps -classpath $CLASSPATH org.apidb.dataload.ResourceXmlValidator $args";

system($cmd);
